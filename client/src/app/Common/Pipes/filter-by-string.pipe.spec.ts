import { FilterByStringPipe } from './filter-by-string.pipe';

describe('FilterByStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByStringPipe();
    expect(pipe).toBeTruthy();
  });
});
