import { SortCardsPipe } from './sort-cards.pipe';

describe('SortCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
