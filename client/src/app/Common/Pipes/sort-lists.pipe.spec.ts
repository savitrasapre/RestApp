import { SortListsPipe } from './sort-lists.pipe';

describe('SortListsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortListsPipe();
    expect(pipe).toBeTruthy();
  });
});
